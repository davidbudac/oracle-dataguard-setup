# Fix Plan — 2026-08-05 Review

Companion to [REVIEW_2026-08-05.md](REVIEW_2026-08-05.md) (finding IDs H1–H12, M1–M40, L1–L21
refer to that document). Work is organized into five disjoint file batches (A–E) so they can be
implemented in parallel, followed by a merge review, live testing on dbmint, and a doc sync.

**Global rules for every batch** (violations are regressions):
- AIX 7.2 portability: no `grep -P`, no `\s`, no BRE `\|` alternation, `printf` not `echo -e`,
  `df -Pk`, no `stat -c` without fallback, no `((VAR++))` (use `x=$((x+1))`).
- Any *new* interactive prompt must be `[[ -t 0 ]]`-gated (the E2E suite pipes fixed stdin).
- `build_convert_pairs()` is duplicated in `tests/test_file_name_convert.sh` — if edited, re-copy
  (Test 11 diffs them).
- The visualizer helper block is duplicated byte-identically between `dg_handoff.sh` and
  `primary/10_generate_handoff_report.sh` — if edited, re-copy (`tests/test_visualizer_url.sh` diffs).
- Do NOT edit `CLAUDE.md` (synced once at the end to avoid conflicts). Editing `docs/*` owned by
  your batch is allowed.
- After edits: `bash -n` every touched file, run the unit tests named in your batch.

---

## Batch A — primary workflow (steps 1, 2, 4, 6)
Files: `primary/01_gather_primary_info.sh`, `primary/02_generate_standby_config.sh`,
`primary/04_prepare_primary_dg.sh`, `primary/06_configure_broker.sh`,
`sql/queries/get_redo_log_size.sql`, `sql/queries/get_archive_dest.sql` (if needed).

| ID | Fix |
|----|-----|
| H1 | `get_redo_log_size.sql`: `SELECT CEIL(MAX(BYTES)/1024/1024) FROM V$LOG`. In step 1, also query MIN and warn when MIN <> MAX ("mixed ORL sizes; SRLs sized to the largest"). |
| H2 | Step 1: immediately after the `V$ARCHIVE_DEST` query, if the value is `USE_DB_RECOVERY_FILE_DEST` (or starts with it), set `USE_FRA_FOR_ARCHIVE=YES` and clear `ARCHIVE_DEST_PATH` so the existing FRA-derivation path runs. Ensure the literal can never be persisted as `PRIMARY_ARCHIVE_DEST`. |
| M16 | Step 1: append `|| true` to the three redo-stats query captures (lines ~464/478/503) so the informational section degrades instead of aborting. |
| M13 | Step 2: compare `STANDBY_DB_UNIQUE_NAME` vs `DB_UNIQUE_NAME` case-insensitively. |
| M14 | Step 2: after sourcing `primary_info_*.env`, fail fast (clear message) if `LOG_MODE != ARCHIVELOG` or `REMOTE_LOGIN_PASSWORDFILE` not EXCLUSIVE/SHARED. |
| L11 | Step 2: check the `awk && mv` persist result; on failure `log_error` + `exit 1`, remove the tmp file. |
| M1 | Step 2 regenerate: keep `PRIMARY_LISTENER_PORT`/`STANDBY_LISTENER_PORT` distinct; use the right one per artifact (primary TNS entry, standby TNS entry, standby listener snippet, local_listener). Normal mode may keep a single source value but must emit via the two names. |
| L12 | Step 2 regenerate: say "regenerated derived files from" instead of "configuration written to"; dedupe the doubled per-file log lines. |
| M2 | Step 4: test each alias in the TNS entry file separately; append only missing entries (split the generated file or grep per alias). |
| M5 | Step 4: re-read FORCE_LOGGING / DG_BROKER_START after changing them, before the summary block. |
| M6 | Step 4: when SRL count is sufficient, also compare `MIN(BYTES)` of `V$STANDBY_LOG` vs `REDO_LOG_SIZE_MB`; warn with fix DDL when undersized (do not auto-drop). |
| L13 | Step 4: `is_numeric`-guard `CURRENT_STBY_GROUPS`, `REQUIRED_STBY_GROUPS`, `MAX_GROUP` with clear errors (mirror step 1). |
| M15 | Step 6: before setting `StaticConnectIdentifier`, check `lsnrctl status` output contains the `_DGMGRL` service; if not, warn with the exact `lsnrctl reload` command and require confirmation (TTY-gated; non-interactive: warn and continue). |
| M3 | Step 6: exit 1 when `BROKER_STATUS` != SUCCESS/WARNING at end (after printing all blocks). |
| M4 | Step 6: replace `sleep 10` with a poll of `SHOW CONFIGURATION` (every 10s, up to ~120s, mirroring step 13) accepting SUCCESS, treating ORA-16610/"in progress" as retry. |

Tests: `bash -n` all; `tests/test_file_name_convert.sh`, `tests/test_path_token_remap.sh`,
`tests/test_counter_increment.sh`, `tests/test_grep_portability.sh`.

## Batch B — standby + optional steps (3, 5, 7, 8, 9, 13) + dg_functions
Files: `standby/03_setup_standby_env.sh`, `standby/05_clone_standby.sh`,
`standby/07_verify_dataguard.sh`, `primary/08_security_hardening.sh`,
`primary/09_configure_fsfo.sh`, `primary/13_set_max_availability.sh`, `common/dg_functions.sh`.

| ID | Fix |
|----|-----|
| H5 | Step 7: capture each DGMGRL output, run through `dgmgrl_output_has_error`; on error increment `ERRORS` (config missing → explicit "run step 6" message). Failed `validate_network` → warning + note that validation did not run. |
| H6 | Step 8: split rotation and lock into two sqlplus calls (or two heredocs) with separate rcs. If rotation succeeded but lock failed: keep going — print the lock DDL to run manually, still run password-file propagation + ACTION REQUIRED block, exit 1 at the end with an accurate message. Never wipe `RANDOM_PWD` before telling the user what state SYS is in (still never print the password itself). |
| M12 | Step 8: reword the ORA-16191 check result: absence of errors right after rotation is *expected* (existing connections stay authenticated); make the summary state that transport will fail on next reconnect until the standby copy is refreshed, and drop the contradictory "still works" wording. |
| L6 | Step 8: also refresh the step-1 staging name `orapw<PRIMARY_ORACLE_SID>` on the share (in addition to `_hardened`), so step 3/9 re-runs pick up the rotated file. Step 9: refresh the staged copy instead of "already exists" skip when step 8 marker/timestamp indicates rotation (simplest: always re-stage). |
| M7 | 09/13: for the three mutating DGMGRL captures, run `confirm_approval_action` *before* the capture (explicit call at the call site) or capture stdout only (`2>&1` → separate stderr). Ensure the prompt is visible in `-a` mode. Keep non-approval behavior identical. |
| M8 | 13 (and 09:109, 03:57): move/guard `confirm_proceed` calls: in `--check` mode, record the finding and continue to the preflight summary without prompting; non-TTY stdin → treat as "no" but with an explicit "(non-interactive: aborting; use -n for check mode)" message. Add `[[ -t 0 ]]` gates. |
| M11 | Step 9: detect `V$DATABASE.CDB`; on a CDB require/derive a `C##` username (allow `#` in the validation regex; auto-prefix with confirmation). Non-CDB behavior unchanged. |
| L5 | Step 9: check SYSDG via `V$PWFILE_USERS` (column SYSDG='TRUE' for the user) instead of DBA_ROLE_PRIVS. |
| M9 | Step 3: when per-directory sizes are used and redo paths (`STANDBY_REDO_PATH`, and SRL path when distinct) are on mounts not covered by `STANDBY_DATA_PATHS`, size those mounts too (ORL+SRL requirement) instead of skipping. |
| M10 | Step 5: on RMAN failure, re-print the reset procedure with concrete paths (standby data/redo dirs) before exiting. |
| L15 | Step 5: re-read instance status after mount before the final summary. |
| L16 | Step 5 / `dg_functions.sh` prompt path: reject `"` in the SYS password prompt with a clear message (matching step 9's observer-password check). |
| L14 | Step 7: fix the header comment (standby only) and add an early hostname/connect guard with an actionable message when run elsewhere. |
| L21/M4 | (coordination) Step 13's poll loop stays the reference implementation; do not diverge. |

Tests: `bash -n` all; `tests/test_counter_increment.sh`, `tests/test_grep_portability.sh`,
`tests/test_df_parsing.sh`.

## Batch C — status/diag tools
Files: `dg_status.sh`, `common/dg_local_status_common.sh`, `common/dg_render_common.sh`,
`dg_check_srl.sh`, `docs/DG_STATUS.md`, `docs/DG_CHECK.md`, `tests/test_sid_detection.sh`.

| ID | Fix |
|----|-----|
| H12 | Local engine: when local identity parsing yields no role (instance down / idle), emit an explicit ERROR ("local instance unreachable / not open") → exit 2. |
| H11 | `dg_check_srl.sh`: count thread-unassigned SRLs (`THREAD#=0`) — count per thread as `THREAD# IN (t.thread#,0)` when only one thread, otherwise treat THREAD#=0 as a shared pool counted toward each thread's requirement with a note; MIN size likewise. Never emit DDL when unassigned SRLs already satisfy count+size. |
| M21 | `dg_check_srl.sh`: replace tab-delimited `read` with a non-collapsing delimiter (e.g. `|` + `IFS='|' read`) and default every field. |
| M22 | `dg_check_srl.sh`: empty thread list → ERROR ("could not read V$THREAD/V$STANDBY_LOG"), exit 2, never "OK". |
| L1 | `dg_check_srl.sh` + `dg_status.sh`: arg/usage errors must exit 2 (srl) / a distinct code (status: use 3 for usage errors; document). Guard missing option args (`[[ $# -ge 2 ]]`). Update docs accordingly. |
| M17 | `dg_status.sh`: parse the FSFODB row (`FS_FAILOVER_STATUS`, `OBSERVER_PRESENT`, `OBSERVER_HOST`); when FSFO enabled and observer not present → summary error (align with local tools). |
| M18 | `dg_status.sh`: `REPL_STATE` — if standby unreachable or no lag/seq data collected → `UNKNOWN` (yellow) + warning, never default IN SYNC. |
| M19 | Align broker WARNING severity: dashboard treats `Configuration Status: WARNING` as warning (exit 1), ERROR as error (exit 2) — same as local tools; final-summary coloring to match. |
| M20 | Both renderers: carry member context across lines — remember the last member line; when a subsequent `Error:`/`Warning:` line appears, attribute it (`add_summary_error "member <db>: ORA-…"`). |
| M23 | `dg_status.sh` SID detect: filter locally (`grep 'ora_pmon_' | head -1`) before sed; drop remote-only reliance; keep `2>&1` off the parsed stream. Extend `tests/test_sid_detection.sh` with a banner-polluted fixture. |
| L17 | Local engine: don't display local `dg_broker_start` as the primary's when running on the standby; show it under the local/standby section (or label as unavailable for the peer). |
| L18 | Render `SUMMARY_INFOS` (dim/info lines under the summary). |
| L19 | Recovery-mode icon: non-real-time apply renders `!!` (warn) locally, matching the summary and the dashboard. |
| L20 | Alert-log sections: always print which file was checked; distinguish "file not found" (warn) from "0 matched". |

Tests: `bash -n`; `tests/test_sid_detection.sh` (extended), `tests/test_grep_portability.sh`.
Live (read-only, allowed): run `dg_check_srl.sh -L`, `dg_triage_sid.sh -L`, `bash dg_status.sh`
against the dbmint pair (`ssh oracle@dbmint -p 2201/2202`, SIDs per environment) to sanity-check parsing.

## Batch D — triggers, services, handoff reports
Files: `trigger/*.sh`, `primary/10_generate_handoff_report.sh`, `dg_handoff.sh`,
`sql/queries/get_user_services.sql`, `tests/test_visualizer_url.sh` (sync only).

| ID | Fix |
|----|-----|
| H3 | `dg_handoff.sh`: prefer the `HostName = '...'` property from `SHOW DATABASE VERBOSE`; accept `DGConnectIdentifier` only when it is genuinely easy-connect (`:port` and/or `/service` present). Fix the fallback grep to match `HostName`. |
| H4 | Both handoff scripts: verdict must consider apply-lag sequences (> DG-style threshold → ERROR), switchover status (`FAILED DESTINATION` etc. → ERROR), and broker output (`dgmgrl_output_has_error`-style scan of the captured text, standalone script gets a local copy) → HEALTHY only when nothing fired. |
| M29 | Both: stop parsing a nonexistent "Open Mode" broker line; use `Real Time Query:` from SHOW DATABASE plus (in 10_) direct standby SQL when available; restore the MOUNTED "not usable" warning path; drop the spurious discovery warning in dg_handoff.sh. |
| L3 | Both: display fields keep internal spaces (trim ends only); `dg_handoff.sh` `clean()` strips tabs too. |
| L4 | Both: only report/encode FSFO threshold when FSFO is enabled. |
| M24 | `get_user_services.sql`: exclude `%\_CFG` and `%\_DGMGRL` (escape properly) like dg_handoff.sh does. |
| M25 | `create_role_trigger.sh` + dedicated-user variant: check `V$DATABASE.CDB`; if YES, error out pointing to `create_role_trigger_cdb.sh`. |
| H7 | Dedicated-user variant: validate the prompted password — reject `"`, `` ` ``, `$`, `\` with a clear message (same approach as 09's observer password). |
| M26 | All three trigger scripts: make the `PKG_STATUS=$(…grep…)` extraction failure-safe (`|| true` inside the substitution) so the DBA_ERRORS diagnostics path is reachable. |
| M27 | `create_cdb_service.sh`: add `AND CON_ID = SYS_CONTEXT('USERENV','CON_ID')` to both `V$ACTIVE_SERVICES` counts (PL/SQL start guard + verification). |
| M28 | `10_generate_handoff_report.sh`: mark the injected default `DB_UNIQUE_NAME` service as "admin/default — NOT managed by the role trigger"; only claim "safe to hand to applications" for services the trigger actually manages. |
| M30 | Trigger prompts: resolve manually-entered service names against `DBA_SERVICES`/`V$ACTIVE_SERVICES` (case-insensitive match → canonical name; not found → re-prompt/confirm). Allow hyphens in the final validation regex (domain-qualified names). |
| L2 | `create_pdb_service.sh`, `create_cdb_service.sh`, `create_role_trigger_cdb.sh`: guard missing option arguments with usage text. |

Sync the visualizer helper block byte-identically if touched; run `tests/test_visualizer_url.sh`.
Live (read-only, allowed): run `./dg_handoff.sh -o /tmp/handoff_test.md` on the dbmint primary and
inspect hostnames/verdict; do NOT deploy triggers or create services yet.

## Batch E — infra (NFS, wallet, observer, cleanup) + migration subproject
Files: `nfs/*.sh`, `common/setup_dg_wallet.sh`, `common/cleanup_nfs_artifacts.sh`,
`fsfo/observer.sh`, `migrate_noncdb_to_pdb/**`.

| ID | Fix |
|----|-----|
| H8 | `nfs/02`: run the write test as the oracle user (`su - oracle -c` / `sudo -u oracle`), fall back with a clear root_squash explanation; guard the `chmod`/`chown` with `|| true` + warning. |
| L10 | `nfs/02`: mount with `-o "$FSTAB_OPTIONS"` so what is tested is what is persisted. |
| L9 | `nfs/01`: make comment and mode agree — keep 750 but correct the comment (share must be group-readable for oinstall workflows) OR tighten to 770-owner-only if oracle-only; pick 750 + fixed comment (least disruptive). |
| H9a | migration 05: add `AND status <> 'RESOLVED'` to the violation check. |
| H9b | migration 05: record `verify_done=true` only when `FAIL==0`; 06 must also refuse when `verify_failures != 0`. |
| H9c | migration 05: fix the applied-SCN query (`V$ARCHIVE_DEST_STATUS.APPLIED_SCN` or `V$RECOVERY_PROGRESS`); make the standby checks honest — connect to the standby TNS when available, else label output "(queried on primary — standby state not verified)" and do not log_success for standby presence. |
| H10 | migration run_minimal.sh: `WHENEVER SQLERROR EXIT SQL.SQLCODE` (+ `-L`) in every sqlplus heredoc; check rc between phases. |
| M35 | migration `_lib.sh`: `run_dgmgrl` returns non-zero when output matches ORA-/ORA-16xxx failure patterns (reuse the `dgmgrl_output_has_error` logic — copy it in, subproject is standalone). |
| M36 | migration 02: `confirm_or_abort` before SHUTDOWN IMMEDIATE (message: takes the source DB offline). |
| M37 | migration 03: pre-scan for duplicate basenames across source datafiles → abort with the list before staging anything. |
| M38 | migration 01: parse lag from SHOW DATABASE output (ApplyLag/TransportLag lines); non-zero lag or unparsable → `fail`/warn honestly; stop advertising checks that don't happen. |
| M39 | migration 01/05: replace `grep -qiE "ORA-|Error"` with the error-pattern check from M35. |
| M40 | migration 03: replace `stat` with `wc -c <` for sizes. |
| M31 | wallet + observer: anchor the sqlnet.ora check: `grep -Eq '^[[:space:]]*WALLET_LOCATION'` (not ENCRYPTION_WALLET_LOCATION); same for the DIRECTORY extraction (parse only the WALLET_LOCATION block). |
| M32 | observer: separate script log from observer stdout (`fsfo_observer_<db>.log` for dgmgrl, `fsfo_observer_<db>_script.log` for the script, or append `>>` for dgmgrl with a start marker); never truncate history. |
| M33 | observer setup: delete-then-create credential (mirror setup_dg_wallet.sh). |
| M34 | observer stop: re-validate PID-is-dgmgrl immediately before TERM and KILL. |
| L7 | wallet: fix the credential listing (parse `mkstore -listCredential` output lines `N: alias user`). |
| L8 | wallet: after configuring, print a note: rotating SYS (step 8) requires re-running wallet setup; stale wallet + WALLET_OVERRIDE masks typed passwords. |
| L2 | wallet `-w` / cleanup `-c`: guard missing option args with usage. |

**Observer caution:** on dbmint the standby's TDE keystore lives in the observer's default wallet dir
(`$ORACLE_HOME/network/admin/wallet`) — never delete/replace wallet files in live tests there.

Tests: `bash -n` all; `tests/test_grep_portability.sh`, `tests/test_counter_increment.sh`,
`migrate_noncdb_to_pdb/tests/run_migration_test.sh` only in the later live phase (mutating).

---

## Phase order

1. **Docs** (this file + REVIEW committed).  ☑ done
2. **Batches A–E in parallel** (disjoint files; read-only live checks only).
3. **Merge review** (fable): read every diff, verify against findings, fix misfires, run all unit
   tests, sweep for global-rule violations.
4. **Live testing on dbmint** (sequential): read-only tools first (dg_status, triage/diag, srl,
   handoff) against cdb1/cdb1_stby; repair the pair if broker is unhealthy (known tnsnames gap);
   then mutating pieces where safe; then the full E2E suite (`tests/e2e/run_e2e_test.sh` with
   JUMP_HOST="" / LOCAL_DEPLOY=true per the environment notes), CDB variant if time allows.
5. **Doc sync**: CLAUDE.md + docs/ updated for changed contracts (step 6 exit code, dg_status usage
   exit code, srl THREAD#0 semantics, step 8 two-phase behavior); update this file's checkboxes.

## Status checklist

- [x] Batch A implemented + unit tests pass
- [x] Batch B implemented + unit tests pass
- [x] Batch C implemented + unit tests pass (incl. live checks; bonus finding H14 fixed)
- [x] Batch D implemented + unit tests pass (incl. live checks on dbmint:2201)
- [x] Batch E implemented + unit tests pass
- [x] Merge review complete (all findings re-verified fixed; step 6 exits 0 on broker
      WARNING - deliberate deviation from the M3 wording)
- [x] Live read-only tools verified on dbmint: healthy ol9_19 pair (dg_status HEALTHY/0,
      triage, dg_check_srl OK both sides, handoff WARNING with named reason) and the
      broken/orphaned pair on :2201 (ERROR verdicts, exit 2, member attribution)
- [x] NFS scripts live-verified as root on the ol9_19 pair (H8 write test passes; new
      finding H13 found + fixed: no self-mount on the server host)
- [x] E2E suite green — the run surfaced and we fixed five MORE live findings on the way:
      H15 (sqlnet NAMES.DEFAULT_DOMAIN breaks generated TNS aliases → step 5 ORA-17627),
      H16 (thread-unassigned SRLs fail step 13's VALIDATE preflight → step 4 now uses THREAD n),
      H17 (step 4 never reloaded the listener; also exposed the E2E cleanup leaving invalid
      SID_DESC husks in listener.ora → TNS-01155 at next reload/restart),
      step 13 now recognizes the benign ORA-01017 static-connect probe under OS auth,
      and a repo-wide sweep replaced all 77 `tr -d ' \n\r'` with tab-safe `tr -d ' \t\n\r'`
      (sqlplus pads numeric output with tabs; one such value broke step 13's archive-dest gate).
      Also: FQDN-tolerant hostname guards (hostnames_match in dg_functions.sh) for steps 3/7,
      and the harness skips the git preflight check under LOCAL_DEPLOY.
- [x] CLAUDE.md / docs synced

## Environment updates discovered during live testing (2026-08-06)

- The old `poug_dg2` standby VM no longer exists; port 2202 now forwards to `ol9_em241`.
  The `cdb1` pair on `poug_dg1` (port 2201) has a permanently unreachable standby - useful
  as a broken-state fixture only.
- The current working DG environment is the Vagrant trio `ol9_19_dg1/dg2/dg3`
  (`/home/db/github/vagrant/dataguard/ol9_19`): SSH ports 2200/2203/2204, hosts
  ol9-19-dg1/2/3.localdomain (192.168.56.111/112/113), pair `cdb1`/`cdb1_stby` at
  MaxAvailability with FSFO + observer on dg3. Root access: vagrant user with the
  node's Vagrant private key, then passwordless sudo.
- `/OINSTALL/_dataguard_setup` NFS share created on dg1 (exported to .111/.112) and
  mounted on dg2 as part of this work.
